"use client";
import { FormEvent, useEffect, useState } from "react";
import { apiCall } from "@/lib/clientApi";
import Toast from "@/components/Toast";

type Settings=Record<string,string>;
export default function PengaturanPage(){const[form,setForm]=useState<Settings>({nama_acara:"Hajatan Gibran Digital",nama_anak:"Gibran Kurniawan",tanggal_acara:"19 September 2026",lokasi_acara:"",pesan_beranda:"Dokumentasi dan pengelolaan acara dalam satu sistem digital."});const[toast,setToast]=useState<{m:string;k:"success"|"error"}|null>(null);useEffect(()=>{apiCall<Settings>("getSettings").then(r=>{if(r.ok&&r.data)setForm(v=>({...v,...r.data}))})},[]);async function submit(e:FormEvent){e.preventDefault();const r=await apiCall("saveSettings",form);setToast({m:r.ok?"Pengaturan berhasil disimpan.":r.error||"Gagal menyimpan pengaturan.",k:r.ok?"success":"error"})}async function backup(){const r=await apiCall<{name?:string}>("createBackup");setToast({m:r.ok?`Backup berhasil dibuat${r.data?.name?`: ${r.data.name}`:"."}`:r.error||"Backup gagal.",k:r.ok?"success":"error"})}return <div>{toast&&<Toast message={toast.m} kind={toast.k} onClose={()=>setToast(null)}/>}<div className="admin-page-heading"><div><span className="eyebrow">Konfigurasi</span><h1>Pengaturan</h1><p>Informasi dasar acara dan pencadangan database.</p></div></div><section className="admin-section-card compact"><div className="card-heading"><div><h2>Informasi acara</h2><p>Data ini dapat digunakan untuk halaman publik.</p></div></div><form className="form-grid" onSubmit={submit}><label>Nama website<input value={form.nama_acara||""} onChange={e=>setForm({...form,nama_acara:e.target.value})}/></label><label>Nama anak<input value={form.nama_anak||""} onChange={e=>setForm({...form,nama_anak:e.target.value})}/></label><label>Tanggal acara<input value={form.tanggal_acara||""} onChange={e=>setForm({...form,tanggal_acara:e.target.value})}/></label><label>Lokasi acara<input value={form.lokasi_acara||""} onChange={e=>setForm({...form,lokasi_acara:e.target.value})}/></label><label className="span-2">Pesan beranda<textarea rows={3} value={form.pesan_beranda||""} onChange={e=>setForm({...form,pesan_beranda:e.target.value})}/></label><div className="form-actions span-2"><button className="btn btn-primary">Simpan Pengaturan</button></div></form></section><section className="admin-section-card"><div className="card-heading"><div><h2>Backup Database</h2><p>Buat salinan spreadsheet saat ini ke folder Backup di Google Drive.</p></div><button className="btn btn-primary" onClick={backup}>Buat Backup Sekarang</button></div><div className="notice">Disarankan juga mengaktifkan trigger backup otomatis harian dari Apps Script.</div></section></div>}
